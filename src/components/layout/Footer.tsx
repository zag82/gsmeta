import React from 'react';

interface Props {}

const Footer = (props: Props) => {
  return (
    <footer className='text-center mt-5 smaller'>
      <small>&copy; 2020 Gss+</small>
    </footer>
  );
};

export default Footer;
